import products from '../data/products.json'

type paginationType = {
    page: number,
    limit: number,
    skip: number
}

const filterProducts = () => {
    
}

const getProducts = ({page, limit, skip} : paginationType) => {
    
}